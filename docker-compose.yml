version: '2'

networks:
  app:

services:
  app:
    build:
      context: docker/
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    networks:
      - app
    volumes:
      - ./:/app:delegated
